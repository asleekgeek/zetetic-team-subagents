version: 1
entries:
- query: How does firecrawl-mcp-server work and how do we replicate auto-ingest/auto-learn?
  query_id: firecrawl-mcp-replication
  aliases:
  - firecrawl mcp reverse engineering
  - auto-ingest auto-learn semantic layer
  - firecrawl feedback loop replication
  intent: ingest
  domain: mcp-architecture
  status: fresh
  confidence: high
  summary: 'firecrawl-mcp-server is a stateless proxy over api.firecrawl.dev; its ''learning'' is the
    search-feedback loop (rating+valuableSources+missingContent+querySuggestions) which improves the index
    service-side. We replicated it locally: Cortex (owned index) + query-keyed YAML semantic layer (table
    of contents) carrying gaps+outcome as the local learn signal. Built across 4 phases (P1 tool, P2 skill,
    P3 hooks, P4 agent steer).'
  facts:
  - claim: The MCP server holds no knowledge; all crawl/search/index happens server-side at Firecrawl.
      Value is tool-surface + embedded workflow playbooks + feedback loop.
    confidence: high
    source: firecrawl-mcp-server src/index.ts:337-377,564-715
  - claim: firecrawl_search_feedback's missingContent[] is 'the most important field' — aggregated to
      decide what to index next. First feedback refunds 1 credit; zero-effort rejected; 2-min window;
      terminal on 4xx.
    confidence: high
    source: src/index.ts:1060-1241
  - claim: Research tools encode search -> citation-graph expand (related_papers) -> read_paper verify-before-accept.
    confidence: high
    source: src/research.ts:230-484
  - claim: 'P1 built: tools/semantic_layer.py (data layer, validates source-on-every-fact per §8) + tools/semantic-layer.sh
      (CLI) + memory/semantic-layer.{schema.,}yaml. Verbs: init/query/record/feedback/revalidate/validate.'
    confidence: high
    source: tools/semantic_layer.py:1-271
  - claim: 'P2 built: skills/research/semantic-ingest-loop.md — the 5-step recall->ingest->distill->persist->feedback
      loop, composed into autoresearch-loop Phase 0/4.'
    confidence: high
    source: skills/research/semantic-ingest-loop.md
  - claim: 'P3 built: session-start-research.sh surfaces indexed-topic count + revalidation backlog; session-end-memory-drain.sh
      reminds to record the learn signal when a research session is active.'
    confidence: high
    source: hooks/session-start-research.sh:52-66; hooks/session-end-memory-drain.sh:34-44
  - claim: 'P4 built: semantic layer added as 4th retrieval surface in memory-protocol.md with a recall-before-ingest
      / record-after protocol binding all team agents.'
    confidence: high
    source: rules/agent-reference/memory-protocol.md
  sources:
  - ref: github.com/firecrawl/firecrawl-mcp-server@main
    kind: code
    accessed: '2026-06-22'
    valuable: true
  gaps:
  - topic: 'P2: semantic-ingest-loop skill'
    status: closed
  - topic: 'P3: hook wiring'
    status: closed
  - topic: 'P4: agent steer paragraph'
    status: closed
  - topic: 'Optional: a close-gap verb on the CLI'
    description: Closing a gap currently requires re-recording the entry; a targeted gap-status verb would
      be ergonomic.
    status: open
  outcome:
    rating: good
    valuable_sources:
    - src/index.ts
    - src/research.ts
    query_suggestions: When reverse-engineering an MCP server, read index.ts tool registrations + the
      server instructions field first; that is where the workflow intelligence lives.
  pointers:
    cortex_memories: []
    files:
    - /tmp/firecrawl-reverse-engineering/01-how-firecrawl-mcp-works.md
    - /tmp/firecrawl-reverse-engineering/02-replication-design.md
    notebook: null
  freshness:
    ttl_days: 180
    revalidate_after: '2026-12-19'
    watch: false
  updated: '2026-06-22'
  created: '2026-06-22'
- query: What loop does the Claude Code creator recommend, and does our autonomous-build-loop match it?
  query_id: claude-code-creator-loop-template
  aliases:
  - claude code best practices loop
  - explore plan code verify iterate
  - give claude a way to verify its work
  intent: verify
  domain: agentic-workflows
  status: fresh
  confidence: high
  summary: 'Claude Code''s official best-practices loop = give Claude a pass/fail check, then work->run
    check->read result->iterate until it passes; explore(plan mode)->plan->implement(verify vs plan)->commit;
    plus an adversarial review subagent in a fresh context. Our autonomous-build-loop matches all of it.
    Key refinement it surfaces: the truly-external deterministic gate is a Stop HOOK running the check
    as a script (real exec, blocks the turn; ends after 8 blocks) — the idiomatic fix for a Workflow''s
    inability to exec.'
  facts:
  - claim: 'The loop: give Claude a check that returns pass/fail; it does the work, runs the check, reads
      the result, and iterates until the check passes — without a runnable check, ''looks done'' is the
      only signal and the human becomes the verification loop.'
    source: https://code.claude.com/docs/en/best-practices
    confidence: high
  - claim: 'Four ways to gate the stop: (1) in one prompt; (2) a /goal condition a separate evaluator
      re-checks each turn until it holds; (3) a Stop HOOK that runs the check as a script and blocks the
      turn until it passes (Claude Code ends the turn after 8 consecutive blocks); (4) a verification
      subagent/dynamic workflow where a fresh model tries to refute the result so the worker isn''t the
      grader.'
    source: https://code.claude.com/docs/en/best-practices
    confidence: high
  - claim: 'Recommended workflow is four phases: Explore (plan mode, read-only) -> Plan (detailed) ->
      Implement (code, verifying against the plan, run tests) -> Commit/PR; skip planning when the diff
      fits one sentence.'
    source: https://code.claude.com/docs/en/best-practices
    confidence: high
  - claim: 'Adversarial review step: a subagent reviews the diff in a FRESH context seeing only the diff
      + criteria (not the reasoning that produced it), grading on its own terms; tell it to flag only
      correctness/requirement gaps to avoid over-engineering, since a gap-seeking reviewer always reports
      some.'
    source: https://code.claude.com/docs/en/best-practices
    confidence: high
  - claim: 'Failure pattern ''trust-then-verify gap'': plausible code that misses edge cases; fix = always
      provide verification (tests/scripts/screenshots) and ''if you can''t verify it, don''t ship it''.'
    source: https://code.claude.com/docs/en/best-practices
    confidence: high
  sources:
  - ref: https://code.claude.com/docs/en/best-practices
    kind: docs
    accessed: '2026-06-22'
    valuable: true
  gaps:
  - topic: Our loop should add a Stop-hook deterministic gate (the doc's idiomatic real-exec external
      gate) to replace the in-loop agent-mediated gate
    status: open
  outcome:
    rating: good
    valuable_sources:
    - code.claude.com/docs/en/best-practices
    query_suggestions: Compare every new agent-loop design against this page before building.
  pointers:
    cortex_memories: []
    files:
    - /tmp/loop-research/docs-en-best-practices.md
    notebook: null
  freshness:
    ttl_days: 120
    revalidate_after: '2026-10-20'
    watch: true
  updated: '2026-06-22'
  created: '2026-06-22'
